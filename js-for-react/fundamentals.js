const father = 'Bodrul Alam';
let weather = 'sunny';

// 6 conditions = === !== <= >= < >
// multiple conditions || &&
 
const Number = [1, 2 , 3];

for(let i = 0; i<Number.length; i++){
    const Number = Number[i];
    console.log(Number);
    
}

function mul(a,b){
    const res = a*b;
    return res;
}
const output = mul(4,5);

const student = {
    name: 'Shakib Khan',
    movies: [1,3,4],
    age : 32,
}
const age1 = student.age;
console.log(student.age);
console.log(student['age']);
console.log(age1);


